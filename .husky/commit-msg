#!/usr/bin/env sh

./node_modules/.bin/commitlint --edit "$1"
node ./scripts/update-changelog-from-commit.mjs "$1"

if ! git diff --quiet -- CHANGELOG.md; then
  git add CHANGELOG.md
  mkdir -p .git/codex
  printf '1\n' > .git/codex/changelog-amend
fi
